input:
  mode: single               # single | batch
  single_file: "D:/path/to/your.las"
  batch_dir: "D:/path/to/las_dir"
  recursive: false

output:
  dir: "D:/path/to/output"
  suffix: "_sim"

label_policy:
  reset_all: true
  background: 0
  bump: 1
  depression: 2

ratio:
  abnormal_ratio: 0.05       # 异常总比例（点比例）
  bump_ratio: 0.6            # 凸起占异常比例
  depression_ratio: 0.4      # 凹陷占异常比例

defect:
  radius: [2.0, 4.0]
  dz: [0.5, 2.0]
  smooth_type: gaussian      # linear | quadratic | gaussian | [list]
  margin_ratio: 0.02
  overlap_policy: avoid      # avoid | allow
  max_defects: 500
  max_attempts: 2000
  progress_every: 10

noise:
  std: 0.0

seed: 2025

logging:
  save_log: true
  log_name: run_log.json

scan:
  root_dir: "D:/CloudPointProcessing/PCGSPRO_1740743422/wappe2007@qq.com"
  target_relpath: "lidars/terra_las/cloud_merged.las"
  output_json: "D:/Feng/codes/generate_slope/scan_list.json"
  recursive: true

dedupe:
  enabled: true
  route_regex: "K\\d{1,4}-\\d{1,4}-\\d{1,4}"
  prefer_non_repeat: true
  prefer_no_suffix_digit: true

step2:
  selected_json: "D:/Feng/codes/generate_slope/scan_list.json"
  output_dir: "D:/Feng/ground_points"
